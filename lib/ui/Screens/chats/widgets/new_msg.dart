import 'package:flutter/material.dart';

class NewMsg extends StatelessWidget {
  const NewMsg({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return CircleAvatar(child: Text('3'),radius: 12,);
  }
}
